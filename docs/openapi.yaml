openapi: 3.1.0
info:
  version: 1.0.0
  title: MyApp API
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: API documentation for the Contacts book application

tags:
  - name: contacts
    description: Contact operations

servers:
  - url: http://localhost:3000
  - url: https://мій-задеплоєний-сайт.com

paths:
  /contacts/{contactId}:
    get:
      $ref: ../swagger/paths/contacts/{id}/get.yaml

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
